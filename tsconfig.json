{
  "compilerOptions": {

    /* 基本选项 */
    "target": "ES2022",               // 编译目标 JavaScript 版本
    "module": "CommonJS",            // 模块系统（Node.js 推荐 CommonJS）
    "moduleResolution": "node",      // 模块解析策略
    "outDir": "./dist",              // 输出目录
    "rootDir": "./src",              // 源代码目录
    "strict": true,                  // 启用所有严格类型检查
    "esModuleInterop": true,         // 允许 CommonJS/ES6 模块互操作
    "skipLibCheck": true,            // 跳过第三方库的类型检查（提升编译速度）

    /* 类型检查增强 */
    "noImplicitAny": true,           // 禁止隐式 any 类型
    "strictNullChecks": true,        // 严格的 null/undefined 检查
    "strictFunctionTypes": true,     // 严格的函数类型检查

    /* 实验性支持（可选） */
    "experimentalDecorators": true,  // 启用装饰器（如 Angular/NestJS）
    "emitDecoratorMetadata": true,   // 为装饰器生成元数据

    /* 路径别名（可选） */
    "baseUrl": "./src",              // 基础路径
    "paths": {
      "@/*": ["*"]                   // 示例：将 "@/utils" 映射到 "src/utils"
    },

    "types": ["node"]
  },
  "include": ["src/**/*"],           // 包含的文件
  "exclude": ["node_modules", "dist"] // 排除的文件
}